// TypeScript-specific handler tests.
// When TypeScript handler tests are needed, they follow the same pattern as C# tests
// but with TypeScript definitions, call graphs, and parser-specific behavior.